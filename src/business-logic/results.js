class Results {
  constructor(playerName, numberOfCards, playTime, tries) {
    this.playerName = playerName;
    this.numberOfCards = numberOfCards;
    this.playTime = playTime;
    this.tries = tries;
  }
}

export default Results;
